# Server-only. Copy this file to .env.local and restart pnpm dev.
# Never prefix this secret with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Local Codex demo (uses the CLI login on this computer).
# Enable in your ignored .env.local; never commit credentials.
# BRAND_AI_PROVIDER=codex
# CODEX_BIN=codex
# CODEX_MODEL=gpt-5.4-mini
# CODEX_REASONING_EFFORT=low
