# 检查记录

## 官方原件直接贴合与署名复核（2026-09-06）

用户明确指示“贴入原logo”后，主杯使用核验过的官方透明 PNG 直接裁去透明边、等比缩放并 alpha 合成；字标没有用字体重排或模型重画。局部清理原重绘字标及头部轮廓，保留原定低对比圆耳暗示。实际新图 SHA-256 为 `be3b4158f8bdb559a95dbb9ee4b5ec3ad45cf34406f36c566ec0186e781de4b2`；修改 18,497 像素，指定修改区域之外 0 像素改变。原件、旧图及处理参数全部保留。

本地后期产物明确记录 `kind: local-postprocess`，无新模型或供应商请求 ID；登记只接受当前暂停版本、已实际检查待修图、绑定且核验的官方来源及完整同尺寸新图。新哈希经实际验收后才允许下游引用。`npm test` **461/461**、构建、bundle 与差异检查通过，日志 `outputs/reviews/live-speed/20260906/postprocess-*.log`；HTTP 回归覆盖同版成果保留、实际新图复检和依赖放行。

真实 Grok 首次后期验图返回占位证据和错误哈希，未作为有效验收；恢复一次实际看图后，对极弱圆耳及压缩预览中粉带可见性给出 `needs_revision`。主代理及独立 Agent 分别实际查看原尺寸产物、官方字标和服装原图，并按冻结要求独立判定通过：粉带清晰可见，弱圆耳符合已约定的低对比暗示。完整差异见 `outputs/reviews/live-speed/20260906/independent-cup-review.md`。通过现有 `recordImageBatchReview` 记录署名结论，新增 `reviewer` 与 `reviewHistory` 保留原 CLI 判定，不冒充 Grok 改判；37 项相关批次/导出回归与类型检查通过。

23:24:27Z 已继续真实下游，杯套与杯盖并发生图。本节尚不是全套交付完成证明。

## 实际产图的局部纠正（2026-09-06）

`npm test` **453/453**、`npm run build`、`python3 scripts/validate_bundle.py` 与 `git diff --check` 通过，日志在 `outputs/reviews/live-speed/20260906/quality-correction-*.log`。新增测试验证同版局部纠正保留旧请求/原图/未通过审查，采用新请求实际复检后才释放依赖，复用已有阶段、绑定与已通过预检；全部物料仅因已知图像质量失败受阻时不重复文字总审。文件发布有事务日志，异常时保留明确失败状态，不把不一致清单交给生图。

透明参考改为忠实纯色背景呈现，保留原件与实际提交附件的独立哈希及冻结文件，生图元数据保存预处理映射。实际 Cotti 原 PNG 的 37,513 个不透明像素逐 RGB 保持不变，858×153 尺寸不变；原件未改动。导出回归覆盖原件/呈现附件和质量纠正历史实际图片。测试中的模型与产图仍是夹具，不代替真实逐件验收。

当前实际会话第 2 版已生成首张杯图并实际验图为 `needs_revision`。20:15:38Z 从局部修图恢复，未重跑前八阶段/采集/绑定/预检；新图生成 62.336 秒，20:18:57Z 实际复检仍为 `needs_revision`，因为耳部仍包含头部轮廓，字标原字形保留也无法确认。系统没有再次总审或生成下游，旧图和两次审查完整保留。主代理实际打开两张生成图及官方参考，同样观察到字重差异。

`output/cotti-disney-20260906-review.zip` 是如实标记 `partial` 的审阅包：当前真实已通过交付 0/5，包含两次实图（旧图在纠正历史）、完整方案和来源证据。159 个包内文件、ZIP SHA-256 与完整性核对通过。已请求用户明确选择是否采用官方字标原件的非生成式贴合；尚未执行该编辑。此记录不代表整套物料完成，也不作为受控提速结果。

## 实际运行中的诊断与局部恢复（2026-09-06）

当前代码 `npm test` **439/439**、`npm run build` 与 `python3 scripts/validate_bundle.py` 通过，日志在 `outputs/reviews/live-speed/20260906/correction-*.log`。新增回归覆盖显式低/中档请求与实际 CLI 档位分离、研究交付安全诊断、明确失败物料的一次授权恢复、离线物料包、可选显式画幅，以及生图前局部草稿修订。测试中的供应商和图像均为夹具。

实际会话 `session-1f6e14b9-2910-4b4e-899c-7d6858e9a98b` 已执行真实 Grok 研究、创意、设计、文案、视觉计划与来源核对。实际看图发现将背景挂帽粉结误归角色、深字深底和主视觉画幅不一致；通过已测试的草稿修订接口保存第 2 版，仅校正最终设计/文案/提示词及相关概念描述，保留前八阶段、旧 CLI 输出和修订审计。来源按实际哈希复制继承，不重做采集与素材核对；新版本重新绑定和预检。此处记录尚不是完整产图或交付完成证明。

阶段计时、人工中断/代码加载停顿与真实档位见 [实测报告](outputs/reviews/live-speed/20260906/measured-stages.md)。本轮含中途诊断及代码修订，不作为受控提速对比；最初调用仍使用 high，后续已记录 copy-a 为 low、visual-b 为 low、design-b 重跑为 medium。首个 stdout 不是首模型 token，也不能拆出思考时间。

## 素材并行与无效返工优化（2026-09-06）

原始六个 Skill 与共享执行方法更新到 **1.6.1**，承接下方九步编排优化。第 6 步统一设计完成即启动同版补采，与文案和逐件提示词制作并行；最终绑定仍等正式提示词和实际素材核对完成。生成继续最多 4 个在途请求，实际验图另有默认 2 个并行任务，被下游真实引用的共用图优先验收。补采指令收紧到当前制作物料的实际缺口，不重复携带完整初始品牌资料；仍有每方一次补采调用，没有宣称宿主可自动消除所有无缺口检索。

创意提出与收敛现在明确共同完成一轮筛选：提出阶段交付实际候选和紧凑依据，收敛阶段沿用并仅质疑、修订、排序及成熟化，完整方案只写入 concepts；section 保留修改依据与取舍。旧版两个输入都要求全套赛马、正文及 concepts 重复写方向；没有完整候选记录，不能认定旧版实际完成过两次十二进三，也不宣称本次指令调整已有质量或耗时实测。

纯继续指令（含“我觉得 ok 可以开始生图了”）保留版本与现有设计，不再当成新增标准清空成果；混合新条件及显式重新研究仍走修订。继续不改变历史会话的 autoProduce。若同版预检通过、选中物料全为明确失败或依赖阻塞且零产图，保留预检证据后直接暂停，省略重复文字总审，不伪造最终验收通过。部分有图、状态未知、未通过预检和原文字流程保留相应处理。

- `npm test`：**402/402** 通过；`npm run build`、`python3 scripts/validate_bundle.py`、`git diff --check` 通过。测试、构建、Bundle 日志及边界见 [验证证据](outputs/reviews/parallel-media/20260906/evidence.json)。外部 CLI、网页和生图在测试中均使用本地夹具。
- 集成回归覆盖补采与文案/提示词实际重叠、正式绑定汇合、暂停/改版隔离、恢复不重复采图或生成、四槽持续补位、两项验图及共用图优先、哈希门槛、纯继续与真实新标准、零产图失败分支及恢复。首轮全测因旧测试依靠 3ms 延迟判断并发而误报；改成双方进入后才放行的同步屏障后全测通过，原日志另行保留。
- [库迪咖啡 × 迪士尼旧版审计](outputs/reviews/parallel-media/20260906/cotti-disney-audit.md)确认：9 次总策划交接合计 **395.324 秒**；并行补采到下一步约 **107 秒**且没有新增原图；预检通过后零产图，又做了 **164.043 秒**末审。7 件计划中 4 件进入制作，主杯请求失败、3 件真实依赖被阻塞。不能据此推断供应商失败的具体原因，也不能删掉实际共用图依赖来虚增并行度。
- 本地空闲服务已重新加载，六个 Skill 均为 1.6.1，文本与自动制作配置正常：[加载快照](outputs/reviews/parallel-media/20260906/runtime-loaded.json)。既有项目保留当时冻结的 Skill，不自动重做研究或重试生图。

本轮未新增真实模型或计费生图调用，尚未做优化后的完整供应商链路测速；上述历史耗时不能相加后宣称为实际提速结果。以下保留之前各阶段的验收口径。

## 九步编排优化（2026-09-06）

宿主直接派发专业任务，正常主链由 18 次模型往返降为 9 次专业调用；双方文字研究并行，创意等待双方研究和现有素材汇合点，主阶段依赖深度为 8 层。该计数不含交付修复、素材检索/核对、绑定、逐件审查与生图。

阶段输入保留完整 Skill 和双方研究，只传实际依赖的正文、来源及待确认项；原始上传正文仅发给本方研究，去掉重复公开讨论和废弃草稿。每次阶段及手动重试使用独立 contextId，只有该阶段的一次交付修复可续接；避免 CLI 历史隐式带回全部前文。Grok 保存输入字符数、准备、进程启动、首个 stdout、进程及整体耗时；首个 stdout 不能解释为模型首 token，也无法拆分思考和服务等待。

- `npm test`：**386/386** 通过；`npm run build`、`python3 scripts/validate_bundle.py` 通过。[验证证据](outputs/reviews/orchestration-optimization/20260906/evidence.json)。
- 回归覆盖双方同时进入 provider、创意汇合、成功一方保留、暂停/改版取消双方、预算边界、九次真实子进程激活与独立上下文、同阶段修复、大输入按依赖传递，以及九步界面的并行状态。
- [八阶段离线回放](outputs/reviews/orchestration-optimization/20260906/context-replay-report.md)：剔除旧请求素材数据后，显式输入累计从 311,154 降至 273,749 字符，减少约 12%。新回放关闭自动制作，系统说明仍略有差异；该口径不代表完整 A/B 测速、实际 token 或输出质量。
- 当前测试使用固定结果与本地假 CLI，未重新运行付费全链路测速，也没有修改首轮方向/物料数量策略。此前关于 18 次主调用的记录属于旧编排。当前正在运行的后端未重启，代码需要在该轮结束或用户暂停后重启服务才能用于下一轮。

## 九步自动素材与视觉链路（2026-09-06）

原始六个 Skill 更新至 1.6.0。网页“开始联名协作”在实时模式显式记录 autoProduce，主控仍执行九步：品牌文字研究与双方素材采集并行，第 3 步创意前汇合实际素材核对结果，第 8 步补采、逐件绑定、出图前检查、四槽滚动生成和逐件 CLI 附图验收，第 9 步读取实际结果总审。默认制作 core 与 recommended，optional 保留候选；历史未启用会话不自动发起批量生成。

- `npm test`：**379/379** 通过；`npm run build` 与 `scripts/validate_bundle.py` 通过。测试日志与范围见 [验证证据](outputs/reviews/automatic-media/20260906/evidence.json)。
- 真 HTTP + Runtime + Pipeline + 持久化批队列集成验证了主控九次交接、九个专业阶段、研究支线并行与创意汇合、四并发补位、先逐件看图再总审、原图/产图/证据接口、哈希变化拒绝、暂停保存在途结果、续跑不重复生图、版本隔离、partial 及总审未通过不能完成。外部 CLI 回答、网页与图像服务在这些测试中使用夹具，不冒充真实供应商成品。
- [浏览器实点记录](../tmp/automation-ui-check/REPORT.md)：开始按钮实际发出 autoAdvance/autoProduce，来源页可打开，暂停/继续可用；四张生成中是 7/9，图像通过但总审未通过是 8/9，全部通过才是 9/9；桌面与 390px 窄屏通过。浏览器使用隔离夹具，未启动正式会话。
- 真实 Grok CLI 与 Codex CLI 均读取了实际图片附件并正确识别可见图形。实际 PID、会话 ID、冻结附件及 SHA-256 见 `outputs/provider-visual-probe-8lI5zI/`，不把这个能力测试当作品牌身份验收。
- [真实检索与采集](outputs/grok-discovery-probe-5jfjv7/probe-result.json)：Grok 实际执行网页搜索并发现官方来源页。CLI 网页读取遇本机 fake-IP，宿主从原始 HTML 提取真实 og:image 后成功下载并完整解码 1200×630 JPEG（271,536 字节），来源页与图像原件/哈希已保存。这张图是通用分享插画，不能证明搜索结果所指日历或特定服装，身份状态保持未核验。
- Grok 素材发现使用真实流式工具事件判断是否执行检索；只有过程占位且无网页工具事件时最多恢复一次，每次保留独立 runId。宿主仅对纯 198.18/15 fake-IP DNS 答案使用固定可信 DoH 重新取得公网地址，继续地址固定与 TLS 校验；没有放开私网地址或修改全局网络设置。

本地服务已重启，`/api/runtime` 确认 Grok CLI、图像 provider 与自动制作均启用，六个 Skill 均加载 1.6.0，见 [加载快照](outputs/reviews/automatic-media/20260906/runtime-loaded.json)。开发服务器已忽略输出目录，实际写入两次来源 HTML 不再触发页面重载或未处理的取消异常。

本轮没有新增计费生图请求。来源页只返回应用壳、站点限制、缺少所需版本图或实际身份核对不通过时，会保存真实缺口；不保证所有官网都能直接提供完整素材。以下章节保留各自日期的历史验证状态。

## 开放品类适配（2026-09-05）

六个 Skill 共用 CATEGORY_ADAPTATION 方法，从业务、产品线、用户动作与环境推导本体设计和物料。完整方法由 runtime 实际加载、纳入六个 Skill 的哈希并保存；品牌示例移到按需参考文件。既有研究/设计正文保存适配依据，后续文案、视觉和审查沿用；没有为每个行业注册独立模板或枚举。

- `npm test`：**161/161** 通过，包含共享方法全文进入实际请求与保存快照、首饰/服务/双方共同开发、限定交付范围的下游传递与恢复、画布及导出。测试中的文本服务使用受控返回，不等于已完成所有品类的真实模型九阶段创作。
- `npm run build`、项目 bundle 校验及六个 Skill 的 `quick_validate.py` 均通过；共享引用路径已逐项检查。原顶层 `compatibility` 说明移入字符串 metadata，保留内容并兼容 Skill 格式校验。
- 独立子代理在无本任务对话历史的上下文中，依据 [六组原始输入](tests/category-adaptation-cases.json) 执行文本概念试跑。输出涵盖陌生触读产品、同品牌两种环境、多主营、跨品类探索、三类硬件与限定两张海报；共 9 个子场景、41 项候选、10 段提示词，实际图片 0 张。没有要求执行者复述预定答案，也没有提供评审预期。

主代理已读取全部六份成果：陌生触读片没有被强制归入熟悉行业；市集借还与室内驻场产生不同服务和内容；眼镜、护理、回收与染织修补能组成多主营系列；雨具延伸到声音空间、维修与共创；手机、机箱、单车分别使用本体设计位置且未添加不存在的屏幕或联网功能。限定案例只列两张不同用途海报。

语义检查保留一项定稿注意：限定海报的概念文案把价格/日期列为“待定”，正式消费者画面应由文案与审查阶段删除该占位或替换为已确认内容。试跑没有调用完整文案与质量审查流程，不把这些概念文本宣称为可直接发布的成品。原始结果保留，不用后改稿掩盖观察。

证据：[执行与读取记录](outputs/reviews/category-adaptation/20260905/EXECUTION.md)、[实际输出及哈希清单](outputs/reviews/category-adaptation/20260905/evidence.json)、[自动测试日志](outputs/reviews/category-adaptation/20260905/automated-tests.log)。行为试跑不是穷尽品类验证，也没有验证生图质量、真实型号、工程性能或量产条件。新会话固定三个方向的路由仍未改动；旧会话沿用创建时的 Skill 快照，使用完整新方法需创建新会话并携带所需资料。

## CPA / Astra 切换验证（2026-09-05 追加）

- 当前本地默认：`OPENAI_PROVIDER=cpa`，文本模型与 Responses 顶层模型均为 `gpt-6-astra`，图片模型为 `gpt-image-2`。
- CPA 实时 `/models` 确认 Astra 和 Image 2 均可见；真实文本请求返回预期的 `{"ok":true}`。
- 一次真实生图请求成功，完成时间 `2026-09-05T08:49:40.812Z`；未自动重发图片请求。
- 本地请求 ID：`3424376e-27e5-443d-a85a-22a74462df45`。
- 测试图：[image.png](outputs/images/image-1ec7a2c1-2383-4041-89a5-99253f851c8e/image.png)；[素材元数据](outputs/images/image-1ec7a2c1-2383-4041-89a5-99253f851c8e/asset.json)。
- 内容 SHA-256：`8cbbb0048cda70d3cece491a8518a14faeede62905db28c68e9d6de11967ab35`。
- `npm test`：76/76 通过，包含 5 个 CPA 配置测试；`npm run typecheck` 通过。项目结构校验通过。
- CPA endpoint 和 Key 通过现有 remote-cpa helper 解析；Key 只在进程内存和捕获的子进程管道中传递，不写入项目文件。CPA 模式明确忽略旧网关 Key、地址和 DNS 覆盖，解析失败不会回退到旧网关。
- 本次按用户“直接走 CPA”的要求请求其 Responses 接口，没有修改远端 CPA、账户或 Dashboard 配置。

以下为此前的 Image 2 接入检查记录，保留当时的模型与素材信息。

日期：2026-09-05。环境：Node.js 24.17.0，Python 3.9.6。

## 卡片与真实 Sol 对话更新

`npm run build` 通过；`npm test` 共 **61/61** 项通过。新增检查覆盖结构化卡片校验、双方品牌信息与所选方向汇总、真实调用模型标记、历史会话兼容，以及 Sol 网关忽略 system 时的受信任输出合同传递。构建与测试输出在 `outputs/reviews/card-sol/`。

真实 `gpt-5.6-sol` 会话 `session-bb57d46b-7d17-42a0-bb99-7ab8bb408751` 已完成：首轮创意后追加无新增实物标准，第 2 版选择「预约阅读场」，11 条 A/B 公开发言、六个 Skill、六张结构化卡片均保存。12 次阶段调用中，首次非 JSON 返回失败；兼容修复后显式继续，后续 11 次成功。本次没有生图，方案审查仍列出授权、容量、预约入口等执行待确认项。

浏览器实测 1440×1000 桌面和 390×844 手机，均显示六张真实卡片，无水平溢出；原文弹窗、完整卡片摘要展开、导出和生产构建页面均通过。导出文件逐字等于 API 结果，并包含全部标准、板块与模型记录。手机原文按钮需滚动到可见区域再点击；按实际滚动交互验证通过。浏览器错误为空。凭证未进入前端源文件、构建文件或 API 返回数据，`.env.local` 权限仍为 `0600`。

证据：[真实运行](outputs/reviews/card-sol/live-evidence.json)、[浏览器验收](outputs/reviews/card-sol/browser-evidence.json)、[桌面卡片](outputs/reviews/card-sol/cards-live-grid.png)、[手机卡片](outputs/reviews/card-sol/cards-live-mobile-card.png)。下方保留同日较早阶段的历史记录。

## 网页工作台新增验收

本轮在原生图适配器基础上实现 React/Vite 前端和本地 Node 双品牌运行服务。`npm run build` 通过；`npm test` 共 **55/55** 项通过；`python3 scripts/validate_bundle.py` 通过六项 Skill 及引用检查。测试完整输出位于 `outputs/verification/test-results.txt`。

浏览器实测开发页面 5173 与生产构建页面 4318 均可加载，无浏览器错误或框架错误层。1440×1000 桌面与 390×844 手机视口没有水平溢出，桌面对话输入框完整位于工作区内。已检查 JSON 品牌资料上传解析与保存、实际 Skill 源文弹窗、演示对话、新标准进入第 2 版并使旧方向失效、选择方向后六个板块完成、Markdown 导出包含新标准。通过人为延迟回包确认新草稿不被清空、切换历史后旧响应不覆盖当前项目、方案画布从顶部打开。

真实文本模型 `deepseek-v4-flash` 已从虚构品牌资料走完九个阶段、六项 Skill 的完整文本流程，生成「杯上选书人」方案。十次阶段调用中九次成功，一次因开发热重启中断后显式恢复；没有模型格式或 HTTP 错误，本轮未新增生图调用。会话 `session-f7f46ec9-13bb-47dd-ba83-df5354100658` 已保存，可从历史项目打开。详细证据与内容边界见 [工作台验收记录](docs/WORKBENCH.md#真实文本端到端验证记录)。

当前仍未完成独立图像检查、完整产物 Schema/MCP 工具协议、模板海报排版和多用户生产部署。网页中的视觉制作阶段先生成制作计划；实际出图需要用户单独点击。下方“尚未验收”部分为本轮网页加入前的生图阶段历史记录，当前网页与文本验证以本节为准。

页面截图：[workbench-live.png](outputs/verification/workbench-live.png)。

## 模板包

`python3 scripts/validate_bundle.py` 通过：6 个 Skill frontmatter、6 个局部参考文件、9 个项目 JSON 文件、白名单一致性、虚构事实假设标记、风格与模板引用、模板边界、12 个验收用例定义及 Markdown 围栏。检查排除依赖目录与生成输出。

## Image 2 API 实现

`npm run typecheck` 通过，开发依赖已精确固定并写入 `package-lock.json`。

`npm test`：38 项测试通过（20 项响应解析、18 项配置与本地 HTTP 集成）。覆盖模型分离、鉴权及 endpoint、SSE/JSON 最终图、参考图编辑请求、图片与元数据保存、失败记录、未完成图片不冒充成功、超时不重试、完整最终图之后断流或超时的保留、显式失败优先、HTTP 408/499/5xx 未知状态、并发拒绝、无重定向跟随及写盘失败前阻断请求。测试不使用真实 Key 或付费接口。

凭证扫描确认提供的 Key 仅出现在 `.env.local`；该文件权限为 `0600`，被项目与父目录的 `.gitignore` 忽略。

## 真实网关验证

`npm run image:check` 成功：网关 Key 有效，目录中包含 `gpt-image-2`、`gpt-5.5`、`deepseek-v4-flash`。目录可见性不代表每个模型都完成生成验证。

已实际执行 **1 次计费生图请求**：`POST /v1/responses`，顶层 `gpt-5.5` 调用 `gpt-image-2`，无参考图，提示词为暖色背景中无品牌陶瓷杯的 API 测试图。未对真实接口自动重试。

- 完成时间：2026-09-05 14:27:47（香港时间）。
- 素材：`image-8b862390-d217-4287-b547-3f216c313c26`。
- 本地请求 ID：`2ec9810b-1982-4621-849f-f73f47172f87`。
- 图片：[image.png](outputs/images/image-8b862390-d217-4287-b547-3f216c313c26/image.png)。
- 元数据：[asset.json](outputs/images/image-8b862390-d217-4287-b547-3f216c313c26/asset.json)。
- 文件：PNG，1254 × 1254，1,677,736 bytes；已打开目视核对，画面为单只陶瓷杯。
- SHA-256：`872478a5809511e34bcbd8a9c2f069db0c523d17f4ffef502c1a5edb068b2ad4`。

模型默认的 2K 是提示词偏好，实际像素如上，不宣称精确 2K。图片记录仍保留 `reviewStatus: unverified`，未代替未来的联名设计审核。

本机系统 DNS 解析异常；使用 Tailscale 状态中核实的对应节点地址作为本项目连接覆盖后成功。HTTPS 域名与证书校验始终启用，没有修改系统 DNS 或关闭 TLS 校验。

## 尚未验收

真实参考图编辑调用（已测本地请求契约）、DeepSeek 文本生成、Agent SDK、受控业务工具注册及身份/版本/预算/幂等守卫、海报渲染、品牌条件的图片审核、HTTP 服务、网页与完整业务端到端流程均未验收。`tests/acceptance-cases.json` 仍是 12 个待执行业务场景。

本轮证明 Image 2 API 适配器可用，完整联名创作服务仍需按技术设计继续实现。
