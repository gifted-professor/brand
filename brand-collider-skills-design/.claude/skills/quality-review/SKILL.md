---
name: quality-review
description: >-
  检查联名方案与整套物料的双方贡献、故事完整性、逐件覆盖、限制条件和跨素材一致性；实际读取输出后逐件验收。不把尺寸变体当独立物料，不把提示词当生成证据，不替代授权、生产认证或独立人工验收。
compatibility: 需要联名碰撞器运行时提供本文约定的受控工具和项目上下文。
metadata:
  version: "1.2.0"
  contract-version: "1.0"
---

# 方案与视觉检查

## 输入与输出

可将 researchContext 的风险事件、口径混淆和未解决差异转成当前项目的检查问题。历史案例不证明当前方案违规或失败，不能替代本次产物与真实图片证据。

输入为当前 brief、设计、文案、渲染计划，以及实际输出；输出 ReviewReport。先阅读 [契约](references/CONTRACT.md)。整套物料验收还需阅读 [整套物料交付约定](../visual-production/references/CAMPAIGN_KIT.md)，检查清单与逐件证据；附件验收不新增 ReviewReport.stage 或 verdict。

## 创意赛马与方案成熟度

检查内部候选与逐项评审记录是否真实存在；默认十二个候选、最终最多三个。不能把生成三项后补写的排名称为十二进三。

对候选先看双品牌必要性、消费者所得、明确产物、目标作用路径和关键执行依赖。需要淘汰时写出具体缺陷；旧推荐没有保留名额，不用热闹程度或综合分掩盖贴标、矛盾与弱价值。

对入围方案检查产品组成、完整参与过程、双方收益、最小执行、经济逻辑、视觉场景、验证方式和未知项是否齐全。若只有一至两个合格就保留实际数量，零个合格就说明缺口。评审附件不是新的 ReviewReport.stage，也不等于出图前通过。

## 出图前

1. 从实际可用上下文读取当前有效条件与精确产物引用；mcp__collider__get_context 可用时才调用，不编造逻辑工具结果。
2. 检查双方贡献是否具体，产品是否只贴两个 Logo，入围方案是否缺乏实质差异，当前深化是否回应了赛马评审的问题。
3. 比较条件、组件字段、描述和视觉提示，寻找“声明不印刷但画面要求印花”等矛盾。
4. 检查已有资源引用及其语义，不接受凭空制造的现有物料。
5. 检查文案是否增加未设计的赠品、价格、日期或官方合作声明。
6. 提交 pre_render 报告，严重问题给出具体修复点。

## 整套交付覆盖

逐项对照当前授权清单：默认效果探索阶段，检查杯托、饮品、折页、独立海报等已约定物料是否分别有真实可评审的效果图，总览板不能替代这些逐件输出。物料家族与画幅变体分开计数；未约定的家族不为凑数量强加。尺寸、刀版、结构展开及生产规格仅在用户进入相应阶段后纳入必验项，本阶段未提供不判为缺项。检查叙事有动机、事件、因果、转折和收束，各触点传达不同信息；story 概要不能代替故事线，视频提示词或分镜占位不能算已生成关键帧，更不能算视频。

检查同一角色、产品、包装图形和文案在物料及镜头间是否一致，实际使用的参考来源是否可追溯，核心身份能否识别。当前先验收外观、产品和故事表达是否成立；概念效果与生产验证保持分开。若另含结构示意或生产文件，按它们实际层级标记，不让附加结构内容挤占效果图交付或把生产未知项升级为本阶段阻断。已确认的身份问题不能因画面好看而略过。

统一入口应可打开所有约定输出，并有真实文件路径或服务端素材 ID、哈希、版本和依赖。逐件列出已制作、已检查及未解决问题；部分文件通过不能覆盖其他物料缺失，未生成项不能计入产量。整包结论必须限定实际层级与范围，例如“准备文件齐全，关键帧尚缺”，不得用一个 all-pass 隐藏缺口。

## 出图后

1. 通过实际可用的 mcp__collider__read_asset 或当前宿主的文件/图像读取能力逐件查看真实输出；不编造逻辑工具调用。视频在范围内时检查实际片段与成片，不能仅凭脚本检查视频。
2. 检查组件、配色、产品身份、禁止元素、文字可读性和概念标记。
3. 每项问题引用实际产物或素材，不把提示词当作图片证据。
4. 无视觉能力、读取失败或关键细节看不清时，visualInspection 为 unavailable 或说明局限，结论不能是完整 pass。
5. 提交 post_render 报告，供宿主决定继续修订、待人工审核或完成。

## 判断纪律

同 Agent 自检不是独立评估；模型结论不能覆盖确定性规则。图已生成不代表满足条件，更不代表品牌授权或生产可行。

只报告能观察或能从有效数据验证的事项。缺失信息写 needs_input 或 unverified。

有界修复由当前宿主限额、用户授权范围及具体失败原因决定，不固定为历史上的一轮。用户的新修订指令可以启动相应范围的新修复；不能凭 Skill 自行增加付费限额。每次修复说明目标、证据、可验证变化和停止条件，只复查受影响项。请求状态或计费未知时先核查，不自动重发；限额用尽、缺少必要来源或连续同类失败且无有效改法时停止相应项并返回准确状态，保留其他已完成物料。
